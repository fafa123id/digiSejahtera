networks:
  shared-network:
    external: true

volumes:
  app_storage:
  laravel_cache:

services:
  app-digisejahtera-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: app
      args:
        GIT_HASH: ${GIT_HASH:-unknown}
    container_name: digisejahtera_app_dev
    restart: unless-stopped
    working_dir: /var/www/digisejahtera
    env_file:
      - ./.env
    volumes:
      - app_storage:/var/www/digisejahtera/storage
      - laravel_cache:/var/www/digisejahtera/bootstrap/cache
      - ./php.ini:/usr/local/etc/php/conf.d/custom.ini:ro
    networks:
      - shared-network

  nginx-digisejahtera-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: web
    container_name: digisejahtera_nginx_dev
    restart: unless-stopped
    environment:
      VIRTUAL_HOST: digisejahtera-dev.s1gro.online
      LETSENCRYPT_HOST: digisejahtera-dev.s1gro.online
      LETSENCRYPT_EMAIL: ahmadfauzanroziqin@gmail.com
    networks:
      - shared-network
    depends_on:
      - app-digisejahtera-dev